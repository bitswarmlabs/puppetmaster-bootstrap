---
classes:
  - packer::aws
#  - packer::aws::networking
#  - packer::aws::users
#  - packer::ec2::cloudinit
#  - packer::ec2::sshd

ohmyzsh::plugins:
  - aws

hello_worlds: 'hello world! we know we are on AWS'